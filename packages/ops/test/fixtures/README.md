# Fixtures

`the-hamlet.storylets/` is a PINNED copy of the Hamlet example as committed on
2026-09-03, for the tests that count its cards, reads and writes. The live
example under `examples/` is authored content and moves; a test that read it
broke the day its conditions changed (2026-09-04). Refresh this copy on purpose,
with the expectations, never by accident.
